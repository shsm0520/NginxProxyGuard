# Nginx Proxy Guard - Environment Configuration
# Copy this file to .env and modify as needed

# ===========================================
# Required Settings (Auto-generated recommended)
# ===========================================

# Database password - MUST change in production!
DB_PASSWORD=change-me-use-openssl-rand-base64-24

# JWT secret for API authentication - MUST change in production!
JWT_SECRET=change-me-use-openssl-rand-hex-32

# ===========================================
# Timezone
# ===========================================

# Server timezone (auto-detect: cat /etc/timezone)
TZ=UTC

# ===========================================
# Optional Settings
# ===========================================

# Database settings (defaults work for most cases)
DB_USER=postgres
DB_NAME=nginx_proxy_guard

# Environment mode
ENVIRONMENT=production

# Let's Encrypt email for certificate notifications
ACME_EMAIL=

# Set to true to use Let's Encrypt staging server (for testing)
ACME_STAGING=false

# MaxMind GeoIP2 (optional - for GeoIP blocking)
# Get free license at: https://www.maxmind.com/en/geolite2/signup
MAXMIND_ACCOUNT_ID=
MAXMIND_LICENSE_KEY=
